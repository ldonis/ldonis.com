jQuery(document).ready(function($) {

    /*
     * Inicializa el slider
     */
    $('#slider').bjqs({
        height: 400,
        responsive  : true,
        showcontrols : false,
        showmarkers : false,
        keyboardnav : false,
        hoverpause : false
    });
    
});
